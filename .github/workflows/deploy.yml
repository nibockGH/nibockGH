name: Deploy to GitHub Pages

on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v4 # Actualizado a la versión 4

    - name: Set up Node.js
      uses: actions/setup-node@v4 # Actualizado a la versión 4
      with:
        node-version: '18.x' # Ajusta la versión de Node.js según sea necesario

    - name: Install dependencies
      run: npm install

    - name: Build
      run: npm run build

    - name: Deploy
      uses: peaceiris/actions-gh-pages@v4 # Actualizado a la versión 4
      with:
        github_token: ${{ secrets.GITHUB_TOKEN }}
        publish_dir: ./dist  # Ajusta este path según donde tu build genere el archivo index.html
